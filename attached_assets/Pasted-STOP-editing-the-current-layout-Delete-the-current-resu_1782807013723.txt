STOP editing the current layout.

Delete the current results page layout and rebuild the results page from scratch to match the uploaded reference image.

The current version is wrong because:
- The business title is wrapping into a tall column.
- The business image is too wide and pushing the text.
- The platform ratings are squeezed beside the title.
- The card is too tall.
- The tags are stacked vertically.
- The Trust Score is too far right and the layout is unbalanced.
- The action buttons are falling too low.
- It does not match the reference image.

You must rebuild the featured result card with this exact structure:

FEATURED CARD LAYOUT:
Use CSS grid:

.featured-card {
  position: relative;
  max-width: 1180px;
  margin: 0 auto 14px;
  background: #ffffff;
  border: 1.5px solid #60F0EF;
  border-radius: 12px;
  padding: 34px 22px 22px;
  display: grid;
  grid-template-columns: 330px 1fr 330px 230px;
  gap: 24px;
  align-items: center;
}

Column 1:
Business image only.
The image must be landscape, not huge.

.business-photo {
  width: 330px;
  height: 175px;
  object-fit: cover;
  border-radius: 8px;
}

Column 2:
Business details only.
This column must include:
- Business name
- Address
- Tags in one horizontal row
- Average rating row
- Review Pay line
- Visit website / Call / Directions buttons

Business name must not wrap into many short lines.
Use:

.business-details h2 {
  font-size: 34px;
  line-height: 1.05;
  font-weight: 950;
  max-width: 360px;
  margin-bottom: 8px;
}

Tags must be horizontal, not stacked:

.tag-row {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin: 14px 0;
}

.tag {
  display: inline-flex;
  align-items: center;
  white-space: nowrap;
  border: 1px solid #d8d8d8;
  border-radius: 999px;
  padding: 6px 10px;
  font-size: 11px;
  font-weight: 900;
  background: #fff;
  color: #050505;
}

Action buttons must stay directly under the rating/review pay line:

.action-row {
  display: flex;
  gap: 10px;
  margin-top: 16px;
  flex-wrap: wrap;
}

.action-row button {
  height: 38px;
  border-radius: 8px;
  border: 1px solid #d8d8d8;
  background: #ffffff;
  color: #050505;
  font-weight: 800;
  padding: 0 14px;
}

Column 3:
Ratings by platform only.
This column must be a compact ratings panel.

.platform-panel {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.platform-title {
  font-size: 13px;
  font-weight: 900;
  margin-bottom: 6px;
}

.platform-row {
  display: grid;
  grid-template-columns: 28px 1fr auto auto;
  gap: 10px;
  align-items: center;
  min-height: 38px;
  border: 1px solid #E5E5E5;
  border-radius: 8px;
  background: #fff;
  padding: 8px 10px;
}

.platform-icon {
  width: 28px;
  height: 28px;
  border-radius: 7px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-weight: 900;
  font-size: 14px;
}

.platform-name {
  font-weight: 800;
  font-size: 14px;
}

.platform-rating {
  font-weight: 900;
  font-size: 14px;
  white-space: nowrap;
}

.platform-count {
  color: #5F6368;
  font-size: 13px;
  white-space: nowrap;
}

Column 4:
Trust Score only.

.score-panel {
  border-left: 1px solid #D8D8D8;
  padding-left: 28px;
  text-align: center;
}

.score-number {
  font-size: 62px;
  font-weight: 950;
  color: #050505;
  line-height: 1;
}

.score-number span {
  font-size: 32px;
}

.score-label {
  margin-top: 8px;
  font-size: 13px;
  font-weight: 950;
  letter-spacing: 0.04em;
}

.score-explain {
  margin: 12px auto 20px;
  max-width: 190px;
  color: #5F6368;
  font-size: 13px;
  line-height: 1.4;
}

.report-button {
  height: 44px;
  border-radius: 7px;
  border: none;
  background: #000000;
  color: #ffffff;
  font-weight: 950;
  padding: 0 22px;
}

AI RECOMMENDATION badge:
It must be black and at the top-left of the featured card, like the reference.

.ai-badge {
  position: absolute;
  top: 0;
  left: 18px;
  transform: translateY(-1px);
  background: #000000;
  color: #ffffff;
  font-weight: 950;
  font-size: 13px;
  padding: 9px 16px;
  border-radius: 0 0 6px 6px;
}

IMPORTANT:
Do not put platform ratings next to the business name in a squeezed way.
Do not stack the tags vertically.
Do not use a pale blue trust score background.
Do not let the business name create a tall narrow column.
Do not let the Visit Website button fall below the visible card.
Do not keep the old dashboard layout.

Lower result rows:
Replace the lower similar business section with horizontal rows like the reference.

.result-row {
  max-width: 1180px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 84px 1.5fr 140px 140px 160px 150px 120px 170px;
  gap: 14px;
  align-items: center;
  background: #ffffff;
  border: 1px solid #E5E5E5;
  border-radius: 8px;
  padding: 10px 18px;
  min-height: 76px;
}

Each row should show:
- Logo/photo
- Business name and category/location
- Google rating chip
- Yelp rating chip
- TripAdvisor rating chip
- Average rating and total reviews
- Trust score
- Black VIEW REPORT button

Do not use big dashboard cards below.

Reference visual:
The final page must look like the uploaded reference:
- Header at top
- Search bar centered
- Results heading
- Large horizontal AI recommendation card
- Photo left
- Details middle
- Platform ratings inside the card
- Trust score right
- Similar businesses as horizontal rows below

Use this exact desktop order:

[Business Image] [Business Details] [Ratings by Platform] [Trust Score]

For mobile only, stack these columns.
Desktop must stay horizontal.

After rebuilding, test with a long name like:
“Re/Max Hometown Realtors, Dennis O’Brien”
The title must not break the layout. If the name is long, allow it to wrap to 2 or 3 lines maximum inside the business details column, but it must not push ratings or score out of place.

Use this CSS for long business names:

.business-details h2 {
  overflow-wrap: normal;
  word-break: normal;
  hyphens: none;
}

Do not use grid-template-columns that make the business name column too narrow.